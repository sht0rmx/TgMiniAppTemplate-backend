# Before running the API, execute the following command to generate secrets:
# npm run gen secrets

# API Settings
# HOST: The host your API will run on. '0.0.0.0' makes it available on all network interfaces.
HOST="0.0.0.0"

# PORT: The port your API will listen on.
PORT=8080

# CORS (Cross-Origin Resource Sharing)
# CORS_ORIGINS: The frontend domain or IP address with the port. This is required to enable your API to accept requests from that origin.
CORS_ORIGINS="http://localhost:3000"  # Example: "http://localhost:3000" or "https://your-frontend-domain.com"

# CORS_CREDENTIALS: Set to 'true' if you want to allow sending cookies and credentials with requests.
CORS_CREDENTIALS="true"  # Allow cookies to be sent with requests

# CORS_SUCCESS_STATUS: The status code to return on successful CORS preflight checks.
CORS_SUCCESS_STATUS=200

# Tokens
# BOT_TOKEN: The token for your Telegram bot obtained from BotFather.
BOT_TOKEN="your_telegram_bot_token_here"  # Replace with your Telegram bot token from BotFather

# API_TOKEN_HASH: A token used for authentication in your API. It should start with 'sk_' and be generated using the command `npm run gen secrets`.
API_TOKEN_HASH="sk_your_api_token_hash_here"  # Replace with the token generated from `npm run gen secrets`

# Krypt (Encryption & Security)
# FP_SECRET: A secret token used for encrypting the fingerprint generated by the user's browser. Generate it using `npm run gen secrets`.
FP_SECRET="your_encryption_secret_here"  # Generated using `npm run gen secrets`

# JWT_SECRET: A secret key used to sign and verify JSON Web Tokens (JWT). Generate it using `npm run gen-secrets`.
JWT_SECRET="your_jwt_secret_here"  # Replace with the JWT secret key generated from `npm run gen-secrets`

# JWT_ALG: The algorithm used for encrypting the JWT. 'HS256' is a common choice for symmetric encryption.
JWT_ALG="HS256"

# PostgreSQL Configuration (from docker-compose)
# DB_HOST: The host of your PostgreSQL instance. If using Docker, it could be 'localhost' or the service name defined in your `docker-compose.yml`.
DB_HOST="localhost"  # Use 'localhost' for local development or the Docker service name

# DB_PORT: The port on which your PostgreSQL instance is running.
DB_PORT=5432  # Default PostgreSQL port

# DB_USERNAME: The username to connect to your PostgreSQL database.
DB_USERNAME="your_db_username_here"  # Set to the username defined in Docker Compose or your DB configuration

# DB_PASSWORD: The password for your PostgreSQL user. Use something secure and store it securely.
DB_PASSWORD="your_db_password_here"  # Replace with a secure password

# DB_TABLE: The name of the PostgreSQL database table you will be using for your bot.
DB_TABLE="botdb"  # Set to the name of your database (can be changed in your docker-compose.yml)

# MinIO Configuration (from docker-compose)
# MINIO_HOST: The host of your MinIO instance. If using Docker, this could be 'localhost' or the service name in `docker-compose.yml`.
MINIO_HOST="localhost"  # Use 'localhost' or the MinIO service name from docker-compose

# MINIO_PORT: The port on which MinIO is running.
MINIO_PORT=9000  # Default MinIO port

# MINIO_USERNAME: The access key used to authenticate with your MinIO instance.
MINIO_USERNAME="your_minio_username_here"  # Replace with your MinIO access key

# MINIO_PASSWORD: The secret key used to authenticate with your MinIO instance.
MINIO_PASSWORD="your_minio_password_here"  # Replace with a secure MinIO password

# MINIO_BUCKET: The name of the bucket you want to use in MinIO to store uploads.
MINIO_BUCKET="uploads"  # Name of the MinIO bucket where you store your uploads (can be any name you prefer)
